#include "hello.h"
void helloprint2() {
	// int count = 0;
	int cnt;
	printf("Hello World from funct2!\n");
	for (cnt = 0; cnt < 0x9fffff; ++cnt);
	// char c;
	// while(1) {
	// 	fread(&c, 1, 1, stdin);
	// 	if (c == 0)
	// 		break;
	// 	++count;
	// 	printf("You have pressed %d times", count);
	// }
}
